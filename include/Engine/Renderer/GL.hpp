#pragma once

// Renderer GL module: central OpenGL include to avoid backend-specific duplication.

#ifndef GL_GLEXT_PROTOTYPES
#define GL_GLEXT_PROTOTYPES
#endif

#include <SDL_opengl.h>
#include <SDL_opengl_glext.h>
